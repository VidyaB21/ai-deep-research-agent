def expand_query(query):

    queries = [
        query,
        f"{query} overview",
        f"{query} applications",
        f"{query} future trends",
        f"{query} advantages and challenges"
    ]

    return queries